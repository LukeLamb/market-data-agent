# Backend configuration for Development Environment
# Phase 4 Step 3: Configuration Management & Environment Automation

bucket         = "market-data-agent-terraform-state-dev"
key            = "dev/terraform.tfstate"
region         = "us-west-2"
encrypt        = true
dynamodb_table = "market-data-agent-terraform-locks-dev"

# Workspace configuration
workspace_key_prefix = "environments"